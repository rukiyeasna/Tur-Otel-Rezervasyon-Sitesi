@model List<OtelListeListViewModel>
@{
    ViewData["Title"] = "SehirOtel";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div id="fh5co-blog-section" class="fh5co-section-gray">
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2 text-center heading-section animate-box">
                <h3>@ViewBag.OtelSehri Otelleri</h3>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row row-bottom-padded-md">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-4 col-sm-6">
                    <div class="fh5co-blog animate-box">
                        <a @*asp-action="Index" asp-controller="TurBilgileri" asp-route-id="@item.OtelId"*@><img class="img-responsive" src="~/img/@item.Picture" alt="" style="width:360px; height:320px;"></a>
                        <div class="blog-text">
                            <div class="prod-title">
                                <h3><a @*asp-action="Index" asp-controller="TurBilgileri" asp-route-id="@item.TurId"*@>@item.OtelAd</a></h3>
                                <span class="posted_by">@ViewBag.OtelSehri</span>
                                <span class="comment"><a asp-action="SignIn" asp-controller="Account" data-bs-toggle="tooltip" data-bs-placement="top" title="Favorilerime Ekle"><i class="icon-heart"></i></a></span>
                                <p><a asp-action="OtelBilgileri" asp-controller="Otel" asp-route-id="@item.OtelId" asp-route-otelad="@item.OtelAd">DETAYLAR...</a></p>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
